MODULE Pattern08;
  VAR i: INTEGER;
  
  PROCEDURE P(x: INTEGER; VAR y: INTEGER);
    VAR z: INTEGER;
  BEGIN
    z := x;
    y := z;
  END P;  

BEGIN 
    i := 2;
    P(5, i); 
    ASSERT(i = 5);
END Pattern08.
