MODULE Pattern4;
  TYPE Ptr = POINTER TO Node;
    Node* = RECORD 
      num*: INTEGER;
      num1*: BYTE;
      num2*: BYTE;
      name*: ARRAY 8 OF CHAR;
      next*: Ptr
    END;
  VAR p*, q*: Ptr;
    r*: Node;
    i, j: INTEGER;
BEGIN
  p.num1 := 10;
  p.num := q.num1;
  p.name[i] := "0";
  p.name[1] := "0";
  p.next := q;
  p.next.next := NIL;
  p.next.next.num1 := 2;
END Pattern4.
