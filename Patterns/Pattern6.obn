MODULE Pattern6;
  VAR n*: INTEGER;
    s*: ARRAY 12 OF CHAR;
    x*, y*: REAL;
    i*: INTEGER;
    b*: BOOLEAN;

  PROCEDURE F(x: REAL): REAL;
  BEGIN
    RETURN x
  END F;

BEGIN
  b := x < y+1.0;
  b := x < 0.0;
  b := x < F(1.0);
  b := i < 1
(*
  WHILE s<"10" DO n := n+2 END;
  REPEAT n := n-1 UNTIL n = 0
*)
END Pattern6.
