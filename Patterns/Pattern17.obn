MODULE Pattern17;
PROCEDURE run;
  VAR m, n: INTEGER;
    x, y: REAL;
    b: BOOLEAN; ch: CHAR;

BEGIN
  n := ABS(m);
  y := ABS(x);
  b := ODD(n);
  n := FLOOR(x);
  n := ORD(ch);
  y := FLT(m);
  n := LSL(m, 3);
  n := ASR(m, 8);
  m := ROR(m, n);
  END run;
BEGIN
END Pattern17.
