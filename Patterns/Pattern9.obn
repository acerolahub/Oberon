MODULE Pattern9;
  VAR x, l: REAL;

  PROCEDURE J;
  BEGIN
  END J;
  
  PROCEDURE F(x: REAL): REAL;
  BEGIN
    J;
    x := x + F(x);
    IF x < 1.0 THEN x := F(F(x)) END; 
    (* 
    IF x < 1.0 THEN x := F(x) END;
    *)
    RETURN x
  END F;

BEGIN 
  x := (x+1.0) + F(x);
END Pattern9.
